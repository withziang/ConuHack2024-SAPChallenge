datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model CarRecord {
  id       Int      @id @default(autoincrement())
  date     DateTime
  time     DateTime
  carType  String
  price    Decimal
}
